{% layout 'account' %}
<div class="page-wrapper">{% render 'global_styles' %}
{% section 'navigation' %}
<main li-content-for-layout="" class="main-wrapper" section-wrapper="main.main-wrapper">{% section 'activate_account' %}
</main>{% section 'footer' %}
</div><script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=66e97989bb936bc07385b5e6" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script><script src="{{ 'liquify-starter-template-1-0-f5cb72.js' | asset_url }}" type="text/javascript"></script><script>document.addEventListener('DOMContentLoaded', function() {
    // If animation has already played, stop execution early
    if (sessionStorage.getItem('animationPlayed')) return;
    // Ensure doormat-animation is visible
    document.querySelector('.doormat-animation').classList.remove('hide');
    // Add .no-scroll to <html> and <body> to prevent scrolling during animation
    document.documentElement.classList.add('no-scroll'); // Apply to <html>
    document.body.classList.add('no-scroll'); // Apply to <body>
    // Wait 1000ms before adding the 'play' class to .doormat-animation
    setTimeout(function() {
        document.querySelector('.doormat-animation').classList.add('play');
        // Wait an additional 5000ms, then add the 'done' class to .doormat-animation.play
        setTimeout(function() {
            document.querySelector('.doormat-animation.play').classList.add('done');
            // Remove .no-scroll from <html> and <body> after animation ends
            document.documentElement.classList.remove('no-scroll');
            document.body.classList.remove('no-scroll');
            // Mark the animation as played in this session
            sessionStorage.setItem('animationPlayed', 'true');
        }, 5000);
    }, 1000); // Initial delay before adding .play
});
</script>{% render 'search_javascript' %}
<script>document.querySelector('.open-signup-trigger').addEventListener('click', () => {
  // Add the .open class to .open-signup-trigger
  const trigger = document.querySelector('.open-signup-trigger');
  trigger.classList.add('open');
  // Add the .open class to .newsletter-input-field-hidden
  const hiddenInputField = document.querySelector('.newsletter-input-field-hidden');
  hiddenInputField.classList.add('open');
  // Add the .open class to .newsletter-signup-btn
  const signupBtn = document.querySelector('.newsletter-signup-btn');
  signupBtn.classList.add('open');
  // Add the .open class to .newsletter-form-input after 0.5 seconds
  setTimeout(() => {
    const formInput = document.querySelector('.newsletter-form-input');
    formInput.classList.add('open');
  }, 500); // 500 milliseconds = 0.5 seconds
});
</script><script src="https://unpkg.com/@studio-freight/lenis@1.0.36/dist/lenis.min.js"></script><script>// LENIS SMOOTH SCROLL
let lenis;
if (Webflow.env("editor") === undefined) {
  lenis = new Lenis({
    lerp: 0.3,
    wheelMultiplier: 0.7,
    gestureOrientation: "vertical",
    normalizeWheel: false,
    smoothTouch: false
  });
  function raf(time) {
    lenis.raf(time);
    requestAnimationFrame(raf);
  }
  requestAnimationFrame(raf);
}
$("[data-lenis-start]").on("click", function () {
  lenis.start();
});
$("[data-lenis-stop]").on("click", function () {
  lenis.stop();
});
$("[data-lenis-toggle]").on("click", function () {
  $(this).toggleClass("stop-scroll");
  if ($(this).hasClass("stop-scroll")) {
    lenis.stop();
  } else {
    lenis.start();
  }
});
</script><script>document.addEventListener('DOMContentLoaded', () => {
    if (Shopify.designMode) {
      const style = document.createElement('style');
      style.innerHTML = `
        .slide-into-view {
          opacity: 1 !important;
          transform: translateY(0) !important;
        }
      `;
      document.head.appendChild(style);
      return;
    }
    // Normal scroll animation logic
    const items = document.querySelectorAll('.slide-into-view');
    const observer = new IntersectionObserver((entries, observer) => {
      entries.forEach((entry, index) => {
        if (entry.isIntersecting) {
          setTimeout(() => {
            entry.target.style.opacity = 1;
            entry.target.style.transform = 'translateY(0)';
          }, index * 300);
          observer.unobserve(entry.target);
        }
      });
    }, {
      rootMargin: '0px 0px -10%',
      threshold: 0.3
    });
    items.forEach(item => observer.observe(item));
  });
</script><script>document.addEventListener('cartupdated', function() {
    window.location.href = '/cart';
});
    </script><script>document.addEventListener("DOMContentLoaded", function () {
    document.querySelectorAll(".nav-link-child-wrapper").forEach(wrapper => {
        let count = 1;
        wrapper.querySelectorAll(".nav-child-link-grid .nav_dropdown-link_child").forEach((element, index) => {
            if (element.textContent.trim().match(/^\d+$/)) {
                element.textContent = String(count).padStart(2, '0');
                count++;
            }
        });
    });
});
</script><script>document.addEventListener("DOMContentLoaded", function () {
    const containers = document.querySelectorAll(".nav-child-link-grid");
    containers.forEach(container => {
      const link = container.querySelector(".nav_dropdown-link_child.link");
      if (link && link.textContent.includes("Archived")) {
        container.classList.add("is-archived");
      }
    });
  });
</script><script>document.addEventListener("DOMContentLoaded", function () {
    const section = document.querySelector(".section_drop-list");
    if (!section) {
      console.log("Section not found.");
      return;
    }
    const items = section.querySelectorAll(".nav-link-child-wrapper.drop-list");
    items.forEach((item, index) => {
      // Update the number like 01, 02, 03, etc.
      const numberEl = item.querySelector(".drop-list-number");
      if (numberEl) {
        const paddedNumber = String(index + 1).padStart(2, "0");
        numberEl.textContent = paddedNumber;
      }
      // Add .is-archived class if the title contains "archived"
      const titleLink = item.querySelector("a.nav_dropdown-link_child");
      if (titleLink && titleLink.textContent.toLowerCase().includes("archived")) {
        item.classList.add("is-archived");
      }
    });
    console.log("&#9989; Drop list numbers updated and archived items marked.");
  });
</script><script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script><script>const swiperTeam = new Swiper('.swiper.is-team', {
  slidesPerView: 1.65,
  spaceBetween: 10,
  slidesPerGroup: 1,
  loop: true,
  loopAdditionalSlides: 3,
  centeredSlides: true,
  breakpoints: {
    920: {
      slidesPerView: 3,
      spaceBetween: 10
    },
  },
  //Le Mousewheel permet de faire d&eacute;filer le swiper avec un slide des doigts sur le pad de votre PC
  mousewheel: {
    forceToAxis: true,
  },
  // Navigation arrows
  navigation: {
    nextEl: '.slider_button.is-next',
    prevEl: '.slider_button.is-prev',
  }
});
</script><script>console.log("Script started.");
const swiperWrapper = document.querySelector(".swiper-wrapper");
if (!swiperWrapper) {
    console.error("Swiper wrapper not found.");
} else {
    console.log("Swiper wrapper found:", swiperWrapper);
    const updateVideoStates = () => {
        console.log("Updating video states...");
        const slides = swiperWrapper.querySelectorAll(".swiper-slide");
        slides.forEach(slide => {
            const video = slide.querySelector("video");
            if (video) {
                if (slide.classList.contains("swiper-slide-active")) {
                    console.log(`Playing video on slide with index: ${slide.getAttribute("data-swiper-slide-index")}`);
                    video.play().catch(err => console.error(`Error playing video: ${err}`));
                } else {
                    console.log(`Pausing video on slide with index: ${slide.getAttribute("data-swiper-slide-index")}`);
                    video.pause();
                }
            } else {
                console.log(`No video found on slide with index: ${slide.getAttribute("data-swiper-slide-index")}`);
            }
        });
    };
    // Run initially to set the correct states
    console.log("Initializing video state updater...");
    updateVideoStates();
    // Add an observer to detect changes to the active slide
    const observer = new MutationObserver((mutationsList) => {
        console.log("Mutation detected. Updating video states...");
        updateVideoStates();
    });
    observer.observe(swiperWrapper, { attributes: true, childList: false, subtree: true });
    console.log("Video state updater initialized and observer added.");
}
</script><script>document.addEventListener('DOMContentLoaded', function () {
  console.log('&#9989; DOM fully loaded');
  const url = window.location.href;
  console.log('&#127760; Current URL:', url);
  // 1. If form success URL param exists, show success state
  if (url.includes('?customer_posted=true#contact_form')) {
    console.log('&#127881; Success form submission detected via URL');
    const footerFormInitialState = document.querySelector('.footer-form-initial-state');
    const footerFormSuccessState = document.querySelector('.footer-form-succes-state');
    if (footerFormInitialState) {
      footerFormInitialState.classList.add('succes');
      console.log('&#9989; .succes class added to .footer-form-initial-state');
    }
    if (footerFormSuccessState) {
      footerFormSuccessState.classList.add('succes');
      console.log('&#9989; .succes class added to .footer-form-succes-state');
    }
    const newsletterElements = document.querySelectorAll('[hs-newsletter-show]');
    if (newsletterElements.length > 0) {
      newsletterElements.forEach(el => {
        el.style.display = 'flex';
      });
      console.log(`&#128236; ${newsletterElements.length} newsletter element(s) shown`);
    } else {
      console.log('&#8505;&#65039; No [hs-newsletter-show] elements found');
    }
  }
  // 2. Input formatting &mdash; name & last name (capitalize), email (remove spaces)
  const generalIds = ['name', 'last_name'];
  generalIds.forEach(function (id) {
    const input = document.getElementById(id);
    if (input) {
      input.addEventListener('input', function (e) {
        let val = e.target.value.trim().replace(/\s+/g, '');
        e.target.value = val.charAt(0).toUpperCase() + val.slice(1).toLowerCase();
        console.log(`&#9997;&#65039; Formatting input: #${id} &rarr; "${e.target.value}"`);
      });
    } else {
      console.warn(`&#9888;&#65039; Input field #${id} not found`);
    }
  });
  const emailIds = ['email'];
  emailIds.forEach(function (id) {
    const input = document.getElementById(id);
    if (input) {
      input.addEventListener('input', function (e) {
        e.target.value = e.target.value.trim().replace(/\s+/g, '');
        console.log(`&#9997;&#65039; Formatting email input: #${id} &rarr; "${e.target.value}"`);
      });
    } else {
      console.warn(`&#9888;&#65039; Email input field #${id} not found`);
    }
  });
});
</script>